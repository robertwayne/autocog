import discord
from discord.ext import commands


class $NAME(commands.Cog):
    def __init__(self, client: commands.Bot):
        self.client = client

    @commands.command()
    async def $NAME_LOWER(self, ctx: commands.Context):
        pass


def setup(client: commands.Bot):
    client.add_cog($NAME(client))
